server:
  port: 3001


spring:
  application:
    name: cloud-consul-provider-payment
  cloud:
    consul:
      host: localhost
      port: 8500
      discovery:
        service-name: ${spring.application.name}