server:
  port: 9002

spring:
  application:
    name: nacos-payment-provider
  cloud:
    nacos:
      discovery:
        server-addr: 192.168.6.10:1111

#端口暴露
management:
  endpoints:
    web:
      exposure:
        include: '*'