server:
  port: 9001

spring:
  application:
    name: nacos-provider-payment
  cloud:
    nacos:
      discovery:
        server-addr: 192.168.6.10:1111

#端口暴露
management:
  endpoints:
    web:
      exposure:
        include: '*'
